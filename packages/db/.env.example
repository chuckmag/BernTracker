# Environment variables for packages/db are loaded from the root .env file.
# Copy /.env.example → /.env at the repo root and set your values there.
#
# For direct Prisma CLI commands (outside of `turbo`), the scripts in
# package.json use dotenv-cli to load ../../.env automatically.

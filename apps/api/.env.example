# Environment variables for apps/api are loaded from the root .env file.
# Copy /.env.example → /.env at the repo root and set your values there.
#
# The dev and start scripts use Node's --env-file flag to load ../../.env
# automatically — no dotenv package needed in application code.

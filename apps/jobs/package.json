{
  "name": "@wodalytics/jobs",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "build": "tsc",
    "job": "node --env-file=../../.env dist/index.js",
    "test": "NODE_OPTIONS='--conditions=source' npx dotenv-cli -e ../../.env -- sh -c 'for f in tests/*.ts; do npx tsx \"$f\" || exit 1; done'"
  },
  "dependencies": {
    "@wodalytics/db": "*",
    "@wodalytics/server": "*",
    "node-html-parser": "^7.1.0"
  },
  "devDependencies": {
    "@types/node": "^22.13.9",
    "dotenv-cli": "^8.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.7.3"
  }
}
